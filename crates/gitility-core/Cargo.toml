[package]
name = "gitility-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
# The pinned `gix` family lands here in Milestone 0/1. Pin exactly and never
# let gix types cross this crate's public API (design doc, R5).
